// Firestore is a schemaless NoSQL database, so there are no SQL migrations to
// run. This script is kept as a no-op so the `bun migrate` command continues to
// exist but performs no work.
console.log('Firestore requires no schema migrations. Nothing to do.')
